/**
 * @file DOM snapshot → ViewportIR normaliser.
 */
export { normalizeViewport } from "./normalize";
export {
  parseBackgroundImage,
  parseBoxShadow,
  parseColor,
  parseFontWeight,
  parsePx,
} from "./parse-css";
