# Per-frame visual round-trip artefacts, regenerated by `bun run render-case`.
# The canonical `.fig` SoT lives in
# `packages/@higma-document-renderers/fig/fixtures/<name>/<name>.fig`;
# `cases/<name>/` here is *output-only* and entirely derived, so the
# whole tree is ignored.
cases/

# Local probe output.
.tmp-output/
