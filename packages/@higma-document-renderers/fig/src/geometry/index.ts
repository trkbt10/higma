/**
 * @file Geometry interpretation — shared SoT exports
 */
export {
  mapWindingRule,
  extractUniformCornerRadius,
  clampCornerRadius,
  resolveClipsContent,
  type WindingRule,
} from "./interpret";
