/**
 * @file CMS workspace center pane (collection items table).
 */

import { SiteCmsCollectionView } from "./SiteCmsCollectionView";

/** Backwards-compatible alias: renders the active collection table view. */
export function SiteCmsWorkspace() {
  return <SiteCmsCollectionView />;
}
