/**
 * @file Document types — import from @higma-document-models/fig/domain directly.
 *
 * Domain types (FigDesignDocument, FigDesignNode, FigPage, etc.) live in
 * @higma-document-models/fig/domain. Import them from there at each usage site.
 *
 * This file is intentionally empty; it exists only to preserve the module
 * path for files that have not yet been migrated.
 */
