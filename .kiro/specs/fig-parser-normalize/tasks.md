# Fig Parser Normalize Tasks

- [ ] Keep every indexed declaration in `normalize.ts` represented in
  requirements.
- [ ] Verify `indexion spec align status` for `normalize.ts` has zero drift,
  spec-only, shallow, conflict, and impl-only items.
