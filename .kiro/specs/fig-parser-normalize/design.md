# Fig Parser Normalize Design

## Scope

This spec maps directly to
`packages/@higma-document-io/fig/src/parser/normalize.ts` declarations exposed
by indexion.

## Design

`isKiwiEnumValue` identifies raw Kiwi enum values during fig parser IO
normalization.

## Drift Gate

`indexion spec align status .kiro/specs/fig-parser-normalize/requirements.md packages/@higma-document-io/fig/src/parser/normalize.ts --threshold 0.3 --fail-on any`
