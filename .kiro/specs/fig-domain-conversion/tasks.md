# Fig Domain Conversion Tasks

- [ ] Keep every indexed declaration in `fig-node-conversion.ts` represented in
  requirements.
- [ ] Verify `indexion spec align status` for `fig-node-conversion.ts` has zero
  drift, spec-only, shallow, conflict, and impl-only items.
