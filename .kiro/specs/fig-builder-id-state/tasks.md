# Fig Builder ID State Tasks

- [x] Keep every declaration in `node-id.ts` represented in requirements.
- [x] Verify `indexion spec align status` for `node-id.ts` has zero drift,
  spec-only, shallow, conflict, and impl-only items.
