# Fig Builder ID State Tasks

- [ ] Keep every declaration in `node-id.ts` represented in requirements.
- [ ] Verify `indexion spec align status` for `node-id.ts` has zero drift,
  spec-only, shallow, conflict, and impl-only items.
