# Fig Validator Message Analyzer Tasks

- [x] Keep every indexed declaration in `message-analyzer.ts` represented in
  requirements.
- [x] Verify `indexion spec align status` for `message-analyzer.ts` has zero
  drift, spec-only, shallow, conflict, and impl-only items.
