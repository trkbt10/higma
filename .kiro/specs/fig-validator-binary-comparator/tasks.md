# Fig Validator Binary Comparator Tasks

- [x] Keep every indexed declaration in `binary-comparator.ts` represented in
  requirements.
- [x] Verify `indexion spec align status` for `binary-comparator.ts` has zero
  drift, spec-only, shallow, conflict, and impl-only items.
