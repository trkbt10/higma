# Fig Parser FigImage Tasks

- [ ] Keep every indexed declaration in `fig-file.ts` represented in
  requirements.
- [ ] Verify `indexion spec align status` for `fig-file.ts` has zero drift,
  spec-only, shallow, conflict, and impl-only items.
