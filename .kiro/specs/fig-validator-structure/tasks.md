# Fig Validator Structure Tasks

- [ ] Keep every indexed declaration in `structure-validator.ts` represented in
  requirements.
- [ ] Verify `indexion spec align status` for `structure-validator.ts` has zero
  drift, spec-only, shallow, conflict, and impl-only items.
