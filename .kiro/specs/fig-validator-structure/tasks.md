# Fig Validator Structure Tasks

- [x] Keep every indexed declaration in `structure-validator.ts` represented in
  requirements.
- [x] Verify `indexion spec align status` for `structure-validator.ts` has zero
  drift, spec-only, shallow, conflict, and impl-only items.
