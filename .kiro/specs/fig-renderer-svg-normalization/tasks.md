# Fig Renderer SVG Normalization Tasks

- [ ] Keep every indexed declaration in `src/svg/renderer.ts` represented in
  requirements.
- [ ] Verify `indexion spec align status` for `src/svg/renderer.ts` has zero
  drift, spec-only, shallow, conflict, and impl-only items.
