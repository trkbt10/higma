# Fig Builder Page Ops Tasks

- [ ] Keep every declaration in `page-manager.ts` represented in requirements.
- [ ] Verify `indexion spec align status` for `page-manager.ts` has zero drift,
  spec-only, shallow, conflict, and impl-only items.
