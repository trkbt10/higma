# Fig Builder Page Ops Tasks

- [x] Keep every declaration in `page-manager.ts` represented in requirements.
- [x] Verify `indexion spec align status` for `page-manager.ts` has zero drift,
  spec-only, shallow, conflict, and impl-only items.
