# Fig Builder Node Factory Tasks

- [x] Keep every declaration in `node-factory.ts` represented in requirements.
- [x] Verify `indexion spec align status` for `node-factory.ts` has zero drift,
  spec-only, shallow, conflict, and impl-only items.
