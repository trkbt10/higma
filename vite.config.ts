/**
 * @file Root Vite configuration (for shared tooling only).
 *
 * Individual packages define their own vite.config.ts for builds.
 * This file exists as a fallback for IDE integration.
 */
import { defineConfig } from "vite";

export default defineConfig({});
